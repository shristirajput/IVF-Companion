# Spring Application Name
spring.application.name=companion

# Server Port
server.port=8080

# Database Configuration (MySQL)
spring.datasource.url=jdbc:mysql://localhost:3306/hospital_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=root@123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate Configuration
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# --- Email (SMTP) Configuration ---
# Using a dummy SMTP server (e.g. Mailtrap or Mailhog) for safe local testing
spring.mail.host=sandbox.smtp.mailtrap.io
spring.mail.port=2525
spring.mail.username=your_mailtrap_user
spring.mail.password=your_mailtrap_pass
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# --- Stripe Configuration ---
stripe.api.key=sk_test_51MockStripeKey1234567890abcdefghijklmnopqrstuvwxyz
stripe.webhook.secret=whsec_MockWebhookSecret

# --- Google Gemini AI Configuration ---
gemini.api.key=YOUR_MOCK_GEMINI_API_KEY

# --- File Upload Configuration ---
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Logging Configuration
logging.level.org.springframework.security=INFO
logging.level.com.ivf.companion=DEBUG

# File Upload Configurations (Medical Reports Support)
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# JWT Configuration Parameters
# 256-bit secure key: 32-bytes hex encoded
security.jwt.secret-key=407f3905a96d11eefb20c84b1d6f4526d17b5f13456b3e32bdf675841029c78f
# 1 day expiration (in milliseconds)
security.jwt.expiration-time=86400000

# Web Application Type (servlet = REST API mode)
spring.main.web-application-type=servlet

